@model Medicalstore.Models.Sale_master

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Sale Master</h4>
<hr />
<div class="row">
   
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="row">
                <div class="col-md-6">
            <div class="form-group">
                <label asp-for="sale_id" class="control-label">Enter Sale Id:</label>
                <input asp-for="sale_id" class="form-control" readonly/>
                <span asp-validation-for="sale_id" class="text-danger"></span>
            </div>
            </div>
            <div class="col-md-6">
            <div class="form-group">
                <label asp-for="sale_date" class="control-label">Enter Sale Date:</label>
                <input asp-for="sale_date" class="form-control" />
                <span asp-validation-for="sale_date" class="text-danger"></span>
            </div>
            </div>
            <div class="col-md-6">
            <div class="form-group">
                <label asp-for="cust_id" class="control-label">Enter Customer name:</label>
                <select asp-for="cust_id" class="form-select">
                    @{
                        if (ViewData["customers"] != null)
                        {
                            List<Customer> customersdata = (List<Customer>)ViewData["customers"];
                            foreach (var customer in customersdata)
                            {
                                <option value="@customer.cust_id">
                                    @customer.cust_nm
                                </option>
                            }
                        }
                    }

                </select>
                <span asp-validation-for="cust_id" class="text-danger"></span>
            </div>
            </div>
            <div class="col-md-6">
            <div class="form-group">
                <label asp-for="grand_total" class="control-label">Enter Grand Total:</label>
                <input asp-for="grand_total" class="form-control" />
                <span asp-validation-for="grand_total" class="text-danger"></span>
            </div>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </div>
        </form>
    </div>


<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
